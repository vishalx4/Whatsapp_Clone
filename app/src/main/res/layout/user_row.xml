<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp">


    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/userImage_search"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:src="@drawable/ic_user"
        />

    <TextView
        android:id="@+id/userName_search"
        android:text="UserName"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="10dp"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/userImage_search"
        android:layout_marginLeft="20dp"/>



    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/image_online"
        android:layout_width="25dp"
        android:layout_height="25dp"
        app:civ_border_width="12dp"
        app:civ_border_color="#0fd529"
        android:src="@drawable/ic_profile"
        android:layout_below="@id/userName_search"
        android:layout_marginTop="14dp"
        android:layout_marginLeft="-16dp"
        android:visibility="gone"
        android:layout_toEndOf="@id/userImage_search"
        android:layout_toRightOf="@id/userName_search"
        />

</RelativeLayout>